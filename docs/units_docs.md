Seconds → Milliseconds
seconds × 1000

Minutes → Milliseconds
minutes × 60 × 1000

Hours → Milliseconds
hours × 60 × 60 × 1000

Days → Milliseconds
days × 24 × 60 × 60 × 1000

Weeks → Milliseconds
weeks × 7 × 24 × 60 × 60 × 1000

Years → Milliseconds
years × 365 × 24 × 60 × 60 × 1000
